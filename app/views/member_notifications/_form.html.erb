<%= form_with(model: member_notification) do |form| %>
  <% if member_notification.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(member_notification.errors.count, "error") %> prohibited this member_notification from being saved:</h2>

      <ul>
        <% member_notification.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :member_id, style: "display: block" %>
    <%= form.select :member_id, options_for_select(Member.all.map{|m|[m.first_name, m.id]})%>
  </div>

  <div>
    <%= form.label :notification_id, style: "display: block" %>
    <%= form.select :notification_id, options_for_select(Notification.all.map{|n|[n.description, n.id]})%>
  </div>

  <div>
    <%= form.label :seen, style: "display: block" %>
    <%= form.check_box :seen %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
