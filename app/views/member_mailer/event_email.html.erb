<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <a href="<%= @calendar_link.google_url %>">Add to Google Calendar</a>
    <br>
    <h3 class="card-title"><%= @event.date.strftime('%m/%d') %> - <%= @event.name %></h3>
    <p>
        <%= @event.description %>
        <br>
        Location: <%= @event.location %>
        <br>
        Time: <%= @event.start_time.strftime('%I:%M %p') %> - <%= @event.end_time.strftime('%I:%M %p') %>
        <br>
        Capacity: <%= @event.capacity %>
        <br>
        Points: <%= @event.points %>
    </p>
</body>
</html>