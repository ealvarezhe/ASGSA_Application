<%= form_with(model: member_role, html: { onsubmit: "return confirm('Are you sure you want to proceed?');" }) do |form| %>
  <% if member_role.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(member_role.errors.count, "error") %> prohibited this member_role from being saved:</h2>

      <ul>
        <% member_role.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <label>Member ID</label>
    <p><%= member_role.member_id %></p>
  </div>

  <div>
    <label>First Name</label>
    <p><%= member_role.member.first_name %></p>
  </div>

  <div>
    <label>Last Name</label>
    <p><%= member_role.member.last_name %></p>
  </div>

  <div>
    <%= form.label :role_id, style: "display: block" %>
    <%= form.collection_select :role_id, Role.all, :id, :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
