<%= link_to("Back to List", events_path, :class => 'back-link') %>

<div class="events edit">
  <h2>Edit Event</h2>

  <%= form_for(@event) do |f| %>
    <% # url: event_path(@event), method: 'patch' %>

    <%= render(partial: 'form', locals: {f: f}) %>

    <div class="form-buttons">
      <%= f.submit("Edit Event") %>
    </div>

  <% end %>
</div>
